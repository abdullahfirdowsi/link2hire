/**
 * Polyfills needed by Angular and third-party libraries.
 * Keep this file minimal; import what the app needs.
 */

import 'zone.js';

// For test environments, `zone.js/testing` will be imported by the test runner.
/**
 * Polyfills file for Angular application.
 * Keep minimal imports required by Angular.
 */
import 'zone.js';
